# SEAN & BARB - HANDOFF 2026-09-16 (r377)

THIS SUPERSEDES handoff-2026-09-16-r376.md.

## r377 - MARKET REPORTS BOX REMOVED FROM /relocation/

Commit message: `r377 - remove market reports list on the relocation hub`

1 file, `relocation/index.html`. Removed `div#sb-mr-index-2026` (Market reports,
updated monthly, with the ten median prices) from the dark Next Steps band. The
Next Steps heading and chips stay. Div and section balance unchanged, zero dashes,
200 through the worker, no page errors at 1440 and 412, hash verified after write.
This also clears one stat-rule breach (prices outside the widget and report pages).

Pre-existing, not touched - the Next Steps `.xlink` chips render navy text on the
#0F0F11 band at desktop, which is low contrast. Worth a fix if Sean scopes it.

## sb-social KV usage (not a site file)

Cloudflare warned the account hit 50% of the daily free KV limit. Cause is the
sb-social 15-minute cron - `runScheduler` and `runPendingComments` each called
`queueList`, which lists `q:` and reads every queue record ever saved (posted items
are never pruned). About 96 runs x 2 x N reads a day. Patched bundle delivered as
`sb-social-worker.js` - queue writes go through `qPut` with list metadata
{s: status, c: commentStatus}, the scheduler uses `queueListActive` (one list, reads
only scheduled or comment-pending records) and passes its items to
`runPendingComments`. Legacy records are tagged at most 50 per run. Mock-KV test,
201 records - old path about 400 reads per run, new path 1 list + 1 read once
migrated. Sean deploys it by pasting into the dashboard editor (MCP cannot deploy).
Status of that deploy is unknown at handoff time.

# (r376 notes follow)

THIS SUPERSEDES handoff-2026-09-15-r375.md.
Paste this over handoff-current.md in project knowledge. Everything in the r375
handoff below its STATE OF PLAY (tooling, traps, open items, key IDs) still applies.

## STATE OF PLAY

- **Build of record: r376**, written straight into the repo clone. No ZIP.
- Source for this session was the repo clone itself (r375), staged by path. 149 text
  files staged, 21 written back, all 21 hash-verified after write.
- Commit message: `r376 - internal linking pass, retired pages 301, guide open access`

## WHY

A Google Analytics review flagged low-traffic pages for removal. Checked against the
served HTML: 32 sitemap pages had zero inbound links anywhere on the site (all 12
seller pages, 18 of 19 neighborhood pages, the physician guide, /markets/). They
were unreachable, not failing. Decision: link them in, retire only the genuinely
low-value pages, keep the 10 physician city pages and link them from the city pages.

## WHAT CHANGED (21 files)

1. **/selling-your-home/** - new `#sb-sell-city` band right after the hero ("Sell in
   Your City"), 10 navy chip links to the city seller pages, plus a line linking
   Downsizing and Luxury Home Marketing. Style block `sb-sell-city-2026`. 2 cols on
   phones, 5 at 768+. Band order still alternates (hero, light, dark...).
2. **City pages, neighborhood links** - one `p.sb-nbhd-links` line after the schools
   line in each Neighborhoods section:
   - Winter Garden - all 10 WG community pages (Canopy Oaks first)
   - Orlando - the 8 Dr. Phillips corridor pages plus Lake Nona Estates
   - Dr. Phillips - the 8 corridor pages (Bay Hill, Emerson Pointe, Phillips Landing,
     Vizcaya, Cypress Point, Reserve at Cypress Point, Turtle Creek, Royal Cypress)
   - Lake Nona - Lake Nona Estates
   The other six cities have no community pages in the worker COMMUNITIES map.
3. **All 10 city pages** - `p.sb-phys-link` under the "Search every ... home" link,
   pointing at that city's /physician/ page.
4. **/lifestyles/** - fifth card, Physician Relocation (`lf-card lf-wide`, spans the
   full row at 640+, image /images/physician-hero.webp). Rules added inside the
   existing `sb-lf-cards-2026` media blocks, not a new override block.
5. **Physician guide is open access (Sean's direction).** On
   /lifestyles/physician-relocation/:
   - Mobile - "Open the Guide" is now a real `<a>` to /lifestyles/physician-relocation/guide/
     (crawlable). Book or button tap plays the cover animation and navigates. The
     mobile form panel no longer opens.
   - Desktop book - the email form renderer is replaced by an Open the Guide link
     button (`.pf-open`) plus "Book a 15-minute call instead" to /contact/. Copy
     updated, no email or sign-up language left.
   - The Apps Script lead endpoint is no longer called from this page. Guide opens
     are still pinged by the guide page itself.
6. **Retired with 301s** (worker REDIRECTS map, additive entries):
   - /listings/ and every /listings/* -> /idx/
   - /markets/ and every /markets/* -> /market-report/
   - /properties now goes straight to /idx/ (was a chain through /listings/)
   - 4 news posts removed from insights/posts.json and 301'd:
     seller-pricing-2026 -> /selling-your-home/,
     buyer-market-shift-2026 -> /buying-a-home/,
     downtown-orlando-lake-eola-tower-2026 -> /cities/orlando-homes-for-sale/,
     lake-nona-laureate-park-426-homes-2026 -> /cities/lake-nona-homes-for-sale/.
     The fifth dated post, orlando-health-tupperware-hospital-2026, was kept because
     it serves the physician channel. The two market-shift posts also carried frozen
     stats, which the stat rule forbids.
   - Links to /listings/ repointed to /idx/ on the home page ("All Properties" card),
     /lifestyles/, /buying-a-home/ hero, and 404.html.
7. **New York page moved** to /relocation/new-york-to-florida/ (canonical, JSON-LD
   and breadcrumb URLs updated), 301 from the old /lifestyles/ URL, relocation hub
   links and sitemap updated.
8. **sitemap.xml** - 148 -> 142 URLs.

## VERIFIED

- `node --check` and `esbuild --bundle --format=esm --target=es2022` pass.
- Served through the real worker: all 142 sitemap URLs return 200, every retired
  URL returns the expected 301.
- Inbound link crawl of served HTML: **orphans 32 -> 1**. The one left is
  /listing/, the ?id= detail template, which should not be in the sitemap at all.
  Same crawl on r375 reproduced the GA reviewer's 32, so the method matches.
- No served page links to any retired URL.
- Playwright at 412 and 1440: sell band, lifestyles grid, WG links and physician
  guide rendered and checked; guide link navigates to /guide/ on both; zero page
  errors; no form or email input left in #guide-outer.
- div / section / p / a / style / script balance unchanged on all 21 files.
- Zero Unicode dashes in every changed file.

## FOR SEAN

- Old files still in the repo, now unreachable because the worker 301s first:
  `listings/`, `markets/`, `lifestyles/new-york-to-florida-relocation/`, and the 4
  insights post folders. Deleting them in Explorer is optional cleanup.
- `Claude outputs/` from r370 is still untracked in the repo. Do not stage it.
- After deploy, resubmit the sitemap in Search Console.

## OPEN / NEXT

- /listing/ is in the sitemap but is a template. Remove it from the sitemap.
- Revisit the 10 physician city pages in 60 to 90 days now that they are linked.
- Everything under OPEN / NEXT in r375 still stands (stat leakage on nine pages is
  still the obvious next build).

## ZIP ROTATION

No ZIP this session. Oldest build in Drive is still
`seanandbarb-full-2026-07-23-probate-seo-r87.zip`.
