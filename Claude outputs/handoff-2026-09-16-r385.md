# SEAN & BARB - HANDOFF 2026-09-16 (r385)

THIS SUPERSEDES handoff-2026-09-16-r384.md. Everything in r384 and earlier below
their STATE OF PLAY still applies.

## STATE OF PLAY

- Build of record: r385, written straight into the repo clone. No ZIP.
- Commit message: `r385 - neighborhood pages rebuilt on the buy page design, full global chrome`
- 1 file (_worker.js), sha256 01185fb4dd81c161241087b9371a0447c53421c8dc4845e4b27a11f91ee15280
  verified after write, mtime guard used.
- Base: r384 as it sat in the clone. r382 through r385 can go in one push.

## CHANGES (all in _worker.js, community handler only)

1. **New layout for the 19 neighborhood pages** - after the r382/r383 replace chain, the
   template `<main>` is replaced with a buy-page-style main:
   - Hero `#sb-nbhd-hero` - same markup and CSS as #sb-buy-hero (full-bleed neighborhood photo,
     glass panel, breadcrumb Home > City > Neighborhood, H1 `#lr-h1`, one-line lead, gold
     See Listings and navy Call Us). H1 allowed to wrap for long names.
   - Dark band `#sb-nbhd-about` (#0A1118) - About {label} with the r382 copy.
   - Light band `#sb-nbhd-listings` (#F4F2EF) - heading plus the untouched
     `#ihf-lifestyle-widget` element the template JS renders into. Removed the lr-community-heading
     and lr-community-desc ids so the template JS no longer overwrites these.
   - Dark band `#sb-nbhd-more` (#06101E) - numbered steps-grid of every sibling neighborhood with
     its lead line, plus gold City Homes and outline Market Report buttons. Replaces r384's
     #sb-nbhd-more text links (that r382 copy block is now dead code, left in place).
   - `.sbcta` form card "Thinking About {label}?" posting to the same Apps Script endpoint,
     source "Neighborhood Page - {label}", with the buy page sbcta-js.
2. **New constants** SB_NBHD_CSS (buy page sb-bands-css, section cards, full-bleed, uniform-body,
   and sb-buy-2026 with #sb-buy-hero renamed #sb-nbhd-hero) and SB_NBHD_CTA_JS. SB_NBHD_COPY
   entries gained `l`, the lead line derived from each meta description.
3. **Bug fixed** - community pages returned `applyHeader(applyFooter(html))` directly, so they
   never got serveHTML's global chrome (Playfair type, sb-header-2026 which hides the desktop
   nav under 1200px, nav morph, bottom nav, CTA toggle, lead events, calc sheets). The handler
   now returns through `serveHTML(...)` like every other page.

## VERIFIED

- node --check and esbuild bundle pass. New constants have no backtick or ${. Worker div balance
  unchanged. Zero Unicode dashes in _worker.js source.
- All sitemap pages rendered through the worker. Only the 19 neighborhood pages changed, no
  status changes. Each has 1 main, 1 H1, the IDX widget element, window.__nbhd, the lead form,
  Place schema and the r382 meta description. 340 to 500 words in main.
- Playwright 412 and 1440 against /buying-a-home/ - hero height, H1, band H2, band body, CTA
  heading all match computed size, family and color. No horizontal scroll, no page errors.
- Demos - demo-r385-canopy-oaks, demo-r385-bay-hill.

## NOTE

- One evaluated em dash ships sitewide from the State Tax sheet string ("Florida has no state
  income tax") in the tax calc code. Pre-existing on every page including /buying-a-home/.
  Worth a one-line fix in its own build.

## OPEN / NEXT

- The em dash above.
- Two insights posts still have one inbound link.
- Stats inside FAQ copy on Maitland city, Windermere city, physician relocation, Lake Nona
  seller page. Content rule violation.
- Duplication - state relocation pages 77%, canal-front city pages 65%, seller city pages 58%.
- Thin hubs /cities/, /lifestyles/, /contact/, /about/.
- Double H1 on 10 insights posts. Titles over 60 chars, descriptions over 160.
- Person nodes for Sean and Barb thin. GSC sitemap resubmit after deploy (Sean).
- Carried from r380 - seller city intro frozen claims, Over-$2M tiles, sb-chat-v1 merge,
  AI headshot sign-off, 2026-27 millage refresh.

## ZIP ROTATION

No ZIP this session. Oldest build in Drive is still
`seanandbarb-full-2026-07-23-probate-seo-r87.zip`.
