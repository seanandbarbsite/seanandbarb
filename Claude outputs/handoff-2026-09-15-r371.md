# SEAN & BARB - HANDOFF 2026-09-15 (r371)

THIS SUPERSEDES handoff-2026-09-14-r370.md.
Paste this over handoff-current.md in project knowledge.

## STATE OF PLAY

- **Build of record: r371.** Base for the whole session was r363
  (`seanandbarb-full-2026-09-13-vanguard-valuation-form-r363.zip`).
  r364 through r371 all shipped straight into the repo clone. No ZIP was used.
- **Baseline: 478 files, 617 ZIP entries, 139 directories.**
- **Delivery route (working):** `device_commit_files` by Windows path into
  `C:\Users\Sean\OneDrive\Documents\GitHub\seanandbarb`, hash/size verified
  before and after. Sean commits and pushes in GitHub Desktop, Cloudflare Pages
  auto-deploys. `device_bash` still cannot mount the connected folders (Plan9
  share issue, Windows update Sep 8) and GitHub API access is still not enabled
  for this session, so `device_list_dir` / `device_stage_files` /
  `device_commit_files` remain the only route.
- Container does not persist. Next session: list the Drive folder, then have Sean
  attach the current ZIP. Never build from memory of the code.

## r371 - SEAN'S FIVE ITEMS

Commit message: `r371 - state hero, ghost watermarks, state page closing blocks`

**79 files changed vs r370.**

### 1. Hero photo moved down and zoomed out (9 state pages)

Measured before changing anything. The art is 1600x679 (2.357). The hero box was
1440x612 desktop and 412x440 mobile with `object-fit:cover`, and the fixed header
is 89-97px tall sitting on top of it. Desktop cropped almost nothing but the
header covered the top of both state outlines; mobile cropped 376px of width,
which is what cut the states off.

Fix in `sb-stx-hero-2026`, rewritten in place (not stacked):

- the image leaves absolute positioning and runs `width:100%;height:auto`, so it
  is never cropped at any viewport width
- the section carries `padding-top:100px` (92px at >=1024px), the height of the
  header, so the art starts below the nav and the strip behind the nav is
  `#080D14`
- the gradient moved from the `.stx-ov` div onto `picture::after` so it covers
  exactly the image, not the whole section. `.stx-ov` is now `display:none`
- the panel overlays the art bottom-left at `scale(.72)` on desktop and sits
  below it on the dark strip on mobile, where a 175px-tall image has no room for
  it. Mobile drops the transform and sets the real type sizes instead, so the
  rendered size is unchanged

Result: desktop hero is 92 + 611 = 703px, mobile 100 + 175 + panel. Both states
fully visible at both widths.

### 2. Ghost watermarks removed sitewide

The watermark was a decorative `<span aria-hidden="true">` holding a huge serif
abbreviation at `opacity:0.06` behind a section heading. **137 of them: S&B (33),
FAQs (61), TAX (11), FYPL (10), WTL (10), plus the per-city initials.** None were
in `_worker.js`, so this was an HTML-only pass. 128 removed directly, the other 9
went out with the state page sections below.

The wrapper divs and the CSS rules that styled the spans were left alone. The
rules go inert with the spans gone and removing them is a separate change.
Post-check: **0 ghost watermarks left in any HTML, CSS or JS file.**

### 3. Side by side sections removed (9 state pages)

"<State> vs Central Florida: Side by Side" - the `cmp-row` grid comparing state
income tax, capital gains, estate, inheritance, property, sales tax and
retirement income. Removed with its `px-5 py-10` wrapper. The `.cmp-*` CSS is
left in place, inert.

### 4 and 5. The closing blocks replaced with the home page version

Sean's two screenshots were the cream "Next Steps" chip band with the navy "Let
Sean & Barb Guide You" card, and the old two-column "Sean & Barb" bio band.

**The home page has no Next Steps section and no navy CTA card.** Its closing
run is `#sb-meet` (the bento: logo lockup, Sean and Barb cards with Read More /
Email / Call / Text, and the 60+ / Hundreds / 10 stat bar) followed by the
worker's "Email Us" footer, which every page already gets. So all three old
blocks were replaced by one `#sb-meet`, copied byte for byte out of index.html
with its two style blocks (`sb-meet-bento-2026`, `sb-meet-portrait-2026`) and its
count-up script. That is the home page version for both items.

The pages now close: FAQ -> `#sb-meet` -> Vanguard `sbcta` form -> footer Email Us.

**Internal links were checked before removing the chips.** All six targets
(/buying-a-home/, /cities/, /lifestyles/, /lifestyles/physician-relocation/,
/selling-your-home/, /contact/) still appear 2-3 times per page in the injected
header nav and footer, in the served HTML, so nothing lost a link.

**Count-up floor applied to `sb-meet-count-2026`**, on the home page and the nine
copies, per the r369 rule: `Math.round(target*(0.94+0.06*e))` with the seed at
`target*0.94` instead of "0". A credential animating up from zero is a wrong
number on screen.

## VERIFICATION RUN (all green)

- `node --check _worker.js` and
  `npx esbuild _worker.js --bundle --format=esm --target=es2022` both pass.
  `_worker.js` is unchanged in r371.
- 132 pages return 200 through the real worker (`scripts/serve.mjs`).
- **`#sb-meet` computed-style parity: every measured property identical between
  the home page and the state page** - section, bento grid, box, who, shot, nm,
  lic, bio, more, fig, cap, lockup img, lead, backgrounds, borders, radii, font
  sizes, mask. Zero differences.
- Computed-style audit across all 132 pages in three chunks: **1137 property
  checks, 0 off-system.**
- Contrast run differentially against the r370 build on 10 pages:
  **133 low-contrast before, 112 after, and zero new findings.** Every flagged
  item in r371 was already flagged in r370.
- jsdom 33/33.
- Heading audit: the only headings that disappeared anywhere on the site are the
  four intended ones per state page (Side by Side, Next Steps, Guide You,
  Sean & Barb). The 70 ghost-only files lost no headings.
- div / section / style / script balance compared file by file against r370:
  **0 files moved.**
- Zero Unicode dashes across every text file.
- All JSON-LD blocks on the nine pages still parse.
- Hero verified visually at 1440 and 412 through the real worker.

## SCREENSHOT TRAP WORTH KNOWING

Two things look broken in a Playwright `fullPage` capture and are not:

1. **`.section-heading` renders at opacity 0** unless the page has been scrolled.
   The site reveals headings on an IntersectionObserver. Scroll the page in
   `page.evaluate` before capturing, or measure computed opacity instead of
   trusting the picture.
2. **Elements with `mask-image` and `.avif` sources capture blank** in fullPage
   mode. The `#sb-meet` portraits and the logo lockup both did. An element
   screenshot renders them correctly, and `naturalWidth` confirms they loaded.
   Do not "fix" either one.

## STAT LEAKAGE - VERIFIED, STILL NOT FIXED (the obvious r372)

Nine pages breach the rule that stats live only in the widget and the market
report pages. Confirmed by grep, not taken on report:

- `cities/lake-mary-homes-for-sale/waterfront/` - "+17.9%" as of March 2026
- `cities/dr-phillips-homes-for-sale/over-2-million/` - sold comp 7017 Via
  Carmel, April 2026
- `cities/lake-mary-homes-for-sale/over-2-million/` - July 2026
- `cities/maitland-homes-for-sale/waterfront/` - July 2026
- `cities/winter-garden-homes-for-sale/over-2-million/` - July 2026
- `lifestyles/golf/` and `lifestyles/over-2-million/` - July 2026 in the hero eyebrow
- `schools/` and `insights/` - July 2026 label

Frozen claims in prose, worse than a table because nothing signals they are
dated. The ten market report pages and the two probate county pages also match a
date grep but are legitimate month-over-month comparisons against an August
figure. Leave those alone.

## OPEN / NEXT

- **`Claude outputs/` is in the repo again.** Two screenshots from the r370
  session (`ca-hero.png`, `ca-final.png`, 2.0 MB). Untracked, so GitHub Desktop
  offers them as new files - do not stage them, and delete the folder in
  Explorer. Claude cannot delete it: `device_bash` cannot mount the folder and
  `device_commit_files` only writes. **Never save chat outputs inside the repo.**
- **Pre-existing CSS defect, not introduced here:**
  `lifestyles/physician-relocation/index.html` style block 2 ends at brace depth
  1 - an `@media` opened and never closed. Identical in r363.
- **`SB_TAX_FIX` is dead code.** Its host (`id="m-calc"` / `m-state`) lives in the
  static `#mortgage-sheet` block that `replaceCalcSheets` strips and replaces.
  Safe to delete the constant and `applyTaxFix`.
- **Pre-existing runtime error:** the homepage throws
  `TypeError: Cannot read properties of undefined (reading 'catch')` under an
  offline `fetch` stub. Identical in r363.
- The `.cmp-*` and `.xlink` CSS on the state pages is now unused. Harmless, worth
  a cleanup pass if one happens.
- No external CSS/JS file changed in r371 (all edits are inline in HTML), so no
  `?v=` cache-bust was incremented.
- Carried forward from r353: mobile drawer Tools accordion, mortgage homestead
  school-exemption math and unlabeled millage, buy page desktop-only calculator
  embed, dynamic community/listing routes skipping global style injection, the
  Windermere July 2026 `-20.00%` figure that should be `-17.06%` ($1,157,500),
  the `yoy` to `mom` rename, GSC Validate Fix, Premier Sotheby's sign-off on the
  AI headshot, and `sb-chat-v1.zip` not merged.

## WHAT SHIPPED EARLIER THIS SESSION

- **r364** Vanguard treatment for every widget and form not already converted.
- **r365** Tax calculator: three page-level layers were repainting it over the
  Vanguard JS (`sb-tax-ledger-2026`, an un-named duplicate block on 11 pages,
  `sb-lightcard-fix-2026`). All three aligned.
- **r366** Audit widened from 29 pages to all 132; two more stale `.sb-mrc`
  blocks found.
- **r367** Gold hairlines restored. r364 had removed r327's
  `rgba(212,175,55,0.30)` overrides as "dead" - they were not. 88 declarations
  across 33 files.
- **r368** RPR retired from the nine state pages; `<!--SB-MKT-SLOT-->` and the
  live Stellar widget replace the frozen July 2026 tables. "days in RPR" renamed
  to "days on market" in `insights/posts.json`. RPR now appears in zero text
  files sitewide.
- **r369** Count-up floor at 94 percent on the worker counter and 86 page-level
  counters.
- **r370** Buy-page pattern on the nine state pages: full-bleed bands, gold-edge
  hero panel, band palette remap, eyebrows stripped, Vanguard `sbcta` form.

## TOOLING (container resets - these are gone next session)

- `scripts/serve.mjs` - serves the site THROUGH the real `_worker.js` over HTTP.
- `scripts/vancheck.mjs` - Playwright computed-style audit, ~32 selectors.
  Chromium at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`; pass
  `executablePath`.
- `scripts/contrast_all.mjs` - contrast sweep that writes a sorted findings file,
  for differential comparison against the previous build. Run it against both and
  `comm -13` the two.
- `scripts/parity.mjs` - computed-style diff of one component across two pages.
- `scripts/shot2.mjs` - screenshots that scroll first so reveals fire.
- `scripts/elshot.mjs` - element screenshots, for anything using `mask-image`.
- `scripts/t2.mjs` - jsdom harness, stubs in `beforeParse`.
- `scripts/r371_hero.py`, `r371_ghosts.py`, `r371_sections.py` - the r371 patchers.
- `scripts/r368_state_pages.py`, `r369_state_design.py`, `r369_sbcta_fullbleed.py`.

**Kill stale servers.** A `serve.mjs` holding a port made a test hit the old
bundle and report a working fix as "not applied." `pkill -f serve.mjs`, or use a
fresh port and grep the served HTML for the new code. `nohup` works for starting
it in this container; `setsid` and a bare `&` both got killed.

## TRAPS LEARNED (cumulative, new ones at the end)

9. Check every property the design system specifies, not just colour.
10. "Removed as dead" needs proof. Diff the computed result before deleting an
    override.
11. Audit the whole site, not the pages you edited.
12. A component's own source file is not proof of what renders.
13. jsdom stubs must go in `beforeParse`.
14. The handoff can be stale about the build of record. List Drive first.
15. Deliver into the repo clone, not as a ZIP.
16. Outbound delivery caps at 30 MiB; inbound uploads do not. Never tell Sean his
    upload failed.
17. The site carries 5.74 MiB of unreferenced assets. Do not delete blind.
18. `<\/script>` in raw HTML does not close a script element. Emit
    `'</scr' + 'ipt>'`.
19. A stale dev server will make a working fix look unapplied.
20. Check before retrying after a relay error. The tool says the reply may have
    been lost, not the write. Verify by size and mtime first.
21. Animated numbers need a floor.
22. When Sean says a number looks wrong, measure it.
23. **Measure the image and the box before touching a crop.** The hero's width
    and height attributes said 1600x893; `naturalWidth` said 1600x679. The attrs
    were wrong and reasoning from them would have produced the wrong fix.
24. **A fixed header over a hero is part of the crop.** The desktop hero cropped
    almost nothing and the states were still cut off, because the nav covered
    them. Check what overlays the element, not only the element.
25. **fullPage screenshots lie about scroll-revealed text and masked images.**
    Scroll before capturing, use element screenshots for masks, and measure
    computed values rather than trusting the picture.
26. **Prove a ported component with a computed-style diff against its source
    page**, not by eye. Cheap, exact, and it catches a missing style block.
27. **Run contrast differentially, not absolutely.** An absolute pass flags
    white-on-photo hero text as a failure. What matters is whether the new build
    added a finding the old one did not have.

## ZIP ROTATION

Drive folder "SB Site Builds" id `1Su3eWlLA_7UIP1dQz5Kx_Ly22rE_i13i`.
Claude cannot upload ZIPs or delete Drive files. Oldest build in the folder is
`seanandbarb-full-2026-07-23-probate-seo-r87.zip` - Sean can delete that one.

## KEY IDs AND PATHS

- Base for this session: `seanandbarb-full-2026-09-13-vanguard-valuation-form-r363.zip`
- Repo: `seanandbarbsite/seanandbarb`, branch `main`, clone at
  `C:\Users\Sean\OneDrive\Documents\GitHub\seanandbarb`
- Vanguard worker constants: `MARKET_GRAPH`, `CALC_MORTGAGE`, `CALC_TAX`,
  `CALC_SELLER`
- State page style block IDs: `sb-stx-hero-2026`, `sb-stx-fullbleed-2026`,
  `sb-stx-cta-2026`, `sb-stx-dark-2026`, plus `sb-meet-bento-2026` and
  `sb-meet-portrait-2026` shared with the home page
- Lead endpoint (all forms):
  `https://script.google.com/macros/s/AKfycbzs-zsOdhVg8eqEYyrhujN_Wuh2va_zQieJWk7MXKWDbcPH9Gllt5jkr2nROf7k93OURw/exec`
- Vanguard tokens: page `#0b0f17`, card `#141b29`, panel `#1a2335`, input
  `#080b11`, gold `#d4af37` to `#aa8822`, dark text `#080d0a`, ink `#f1f5f9`,
  muted `#94a3b8`, box border `rgba(212,175,55,0.30)`, outer card edge
  `rgba(212,175,55,0.55)`, divider `rgba(51,65,85,0.5)`
- Brand tokens (page chrome): navy `#002349`, gold `#C4952A` / `#E8B84B`,
  cream `#F4F2EF`. Band palette: `#0A1118`, `#F4F2EF`, `#0F0F11`, `#F4F2EF`,
  `#06101E`, `#F4F2EF`.
