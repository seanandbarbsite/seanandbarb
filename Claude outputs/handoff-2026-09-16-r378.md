# SEAN & BARB - HANDOFF 2026-09-16 (r378)

THIS SUPERSEDES handoff-2026-09-16-r377.md. Everything in r377, r376 and r375 below
their STATE OF PLAY (tooling, traps, open items, key IDs) still applies.

## STATE OF PLAY

- Build of record: r378, written straight into the repo clone. No ZIP.
- Commit message: `r378 - seller city pages on the new sell page design, live market widget`
- 11 files, all hash-verified after write (re-staged and sha256 compared).

## r378 - THE TEN SELLER CITY PAGES

Sean: update all the sell-your-home city pages with the new style, like the buy page,
including the new widget designs.

Model page is `/selling-your-home/`, which already carries the buy-page pattern plus the
seller widgets. Each city page was rebuilt from the hub's own bytes with the city's own
copy dropped in (script `scripts/r378_build.py`, source = the r377 city page).

New page order, alternating bands:
1. `#sb-sell-hero` - city photo in a picture tag (avif + webp), gold-edge `sb-hero-panel`,
   breadcrumb Home > Selling Your Home > City, city H1 and hero line, Get Your Home Value
   and the Call Us button that flips to Email Us outside 9 to 6 Eastern.
2. `#sb-sell-intro` light - the city's "Selling in X Requires Knowing X" copy plus its
   callout as `.sell-pull`. Eyebrows stripped.
3. `#sell-showings-h` dark - No Lockboxes band, hub copy.
4. `#sv-net-calc` light - Vanguard `#svp` net proceeds bento and the `#sv-valuation` form.
   Hidden message names the city.
5. `<!--SB-MKT-SLOT-->` - live Stellar market widget, opens on that city's tab.
6. `#sb-reach` light - hub map visual and marketing plan list, the city's own buyer copy.
7. `#sb-meet` dark - home page bento. Count-up floor 0.94 applied (r369 rule).
8. FAQ light - C1 split panel (`.fx`) with the city's five questions (FAQPage JSON-LD
   unchanged, still matches), plus a links line to the city buy page, its waterfront
   page, the sell hub and the marketing plan.
9. `.sbcta` Vanguard consultation form, city heading and sub, source
   "Sell <City> - Listing Consultation".

Removed: the frozen stat tiles (median, days, per sq ft, a stat-rule breach), the gold
dx showings card, the white `#snc-card` net sheet, the old two-column bio band, the
related-resources cards (now the links line) and the navy Call Us card.

Head is the hub head with the city's title, description, og tags, canonical and its own
WebPage, FAQPage and BreadcrumbList JSON-LD. og:image is the city photo. The hub's
VideoObject JSON-LD is not carried (no videos on city pages).

Not carried from the hub on purpose: the drone tours (Winter Garden footage), the
five-steps panel (would break band alternation), and the overpricing band, because its
tile says "#1 in Luxury Home Sales in Florida / Sotheby's Ranking", which is not the
locked brokerage wording. **That tile is live on /selling-your-home/ and should be fixed.**

### _worker.js (additive, 5 lines)

In `applyMarketSnapshot`, before the r306 slot branch: a `/selling-your-home/<city>/`
path with the slot marker re-enters the function as `/cities/<city>-homes-for-sale/`, so
the widget opens on the city tab with the city headline, median and screen-reader line.

## VERIFIED

- `node --check` and `esbuild --bundle --format=esm --target=es2022` pass.
- Served through the real worker: all 10 pages 200, widget city label correct on each.
- Playwright at 412 and 1440 on all 10 plus the hub: FAQ switching, net calc math
  ($1,000,000 price, $200,000 payoff, 6% plus 1.5% closing = $725,000,
  5% = $735,000, identical to hub), form validation, page URL fill, counters land on
  60+ and 10, zero page errors, no horizontal scroll.
- Hero height and panel position identical to the hub at both widths.
- div, section, p, a, style, script, picture, form balance 0 on every page. No duplicate
  IDs. Every style block brace-balanced. Zero Unicode dashes.
- Only remaining $ figures are durable price bands in FAQ answers.

## OPEN / NEXT

- Fix the non-compliant "#1 in Luxury Home Sales in Florida" tile on /selling-your-home/,
  and add the 0.94 floor to the hub's two counters (`sb-sell-count-2026`,
  `sb-meet-count-2026`), which still start from zero.
- City intro copy still says things like "double-digit year-over-year appreciation".
  Not a stat, but a frozen claim. Worth a copy pass.
- City hero photos are 1000px wide (the hub's is 1600). Fine at 412, soft on large
  desktops. Higher-res art would help.
- Everything under OPEN / NEXT in r377 and r376 still stands.

## ZIP ROTATION

No ZIP this session. Oldest build in Drive is still
`seanandbarb-full-2026-07-23-probate-seo-r87.zip`.
